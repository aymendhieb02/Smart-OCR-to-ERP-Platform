"""Optional Microsoft Table Transformer integration.

The package is intentionally experimental and disabled by default. Importing it
must not initialize heavy model dependencies or alter the deterministic table
engine.
"""
